# Tests

- A change should NEVER reduce our test coverage. If you are adding new code, you should be adding new tests to cover that code.
- Run `make test-all` to check test coverage of all packages after making a change.

# Commits

- Never commit a change without requesting a human review of your changes
- Prefix all commit messages with `(ai)[julia] - <rest of message>`
